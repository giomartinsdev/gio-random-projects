# front

Reserved — no frontend built yet. When one exists, it follows the same
convention as `api`/`worker`: its own Dockerfile here, auto-discovered
and independently deployed by `.github/workflows/apps-deploy.yml`.
