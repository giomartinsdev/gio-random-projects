# Generate: openssl rand -base64 24
POSTGRES_PASSWORD=change_me
POSTGRES_USER=domain
POSTGRES_DB=domain
API_PORT=8000

# Comma-separated key:label pairs — generate a key with:
#   openssl rand -hex 32
DOMAIN_API_KEYS=change_me:my-client

RATE_LIMIT_RPS=1
RATE_LIMIT_BURST=5
