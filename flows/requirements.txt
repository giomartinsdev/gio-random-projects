prefect>=3,<4
pydantic>=2,<3
httpx>=0.27,<1
# Only vehicle_position_archiver uses this — it now owns the "build a
# Parquet file from the rows being archived" logic that used to live in
# api/domain (see flows/vehicle_position_archiver/etl/transform.py).
pyarrow>=17,<26
opentelemetry-distro>=0.50b0
opentelemetry-exporter-otlp>=1.29.0
opentelemetry-instrumentation-httpx>=0.50b0
