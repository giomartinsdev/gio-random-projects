-r requirements.txt
pytest>=8,<9
pytest-asyncio>=0.24,<1
pytest-bdd>=8,<9
pytest-cov>=6,<8
httpx>=0.27,<1
# Pinned below 4.9 deliberately: 4.9+ splits PostgresContainer out into
# a separate "community" spinoff package (testcontainers-postgres) whose
# own testcontainers-core pin conflicts with this testcontainers version
# and breaks basic imports (confirmed: ImportError on
# raise_for_deprecated_parameter from mixing the two). 4.8.x still ships
# testcontainers.postgres directly, no extra package needed.
testcontainers>=4.8,<4.9
ruff>=0.6,<1
pyright>=1.1,<2
boto3-stubs[s3]>=1.34,<2
