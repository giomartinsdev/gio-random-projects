-r requirements.txt
pytest>=8,<9
pytest-asyncio>=0.24,<1
pytest-bdd>=8,<9
pytest-cov>=6,<8
# Pinned to match api/domain's requirements-dev.txt — see its comment;
# not strictly required here (gateway only uses the generic
# DockerContainer, not the postgres community package), but keeping one
# known-good testcontainers version across the repo avoids relearning
# this the hard way a second time.
testcontainers>=4.8,<4.9
ruff>=0.6,<1
pyright>=1.1,<2
