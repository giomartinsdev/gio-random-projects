x-arcane:
  urls:
    - https://gotify.net

services:
  gotify:
    image: gotify/server:${GOTIFY_VERSION:-latest}
    restart: unless-stopped
    ports:
      - "${APP_PORT:-8085}:80"
    environment:
      TZ: ${TZ:-UTC}
    volumes:
      - gotify-data:/app/data

volumes:
  gotify-data:
