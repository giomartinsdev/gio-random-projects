APP_PORT=4200
PREFECT_VERSION=3-latest
# This is what the BROWSER calls, not the container — if you put Prefect
# behind a domain/tunnel/reverse proxy, set this to that public URL
# (e.g. https://prefect.example.com/api), or the UI tries to reach
# localhost on the visitor's own machine and never connects.
# IMPORTANT: no :4200 (or any port) once you're on a domain — the tunnel/
# proxy terminates on 443, nothing public listens on 4200, so a port here
# makes every API call hang forever ("pending" in the Network tab, UI
# stuck loading).
PREFECT_UI_API_URL=http://localhost:4200/api
PREFECT_WORK_POOL=docker-pool
# Flow-run containers the worker spawns must join this network to reach
# prefect-server by name — reference it in your deployment's
# job_variables.networks (see prefect.yaml in the repo this app deploys from).
PREFECT_NETWORK=prefect-net
